export enum TokenType {
  ACCESS = 'access',
  RESET = 'reset',
  REFRESH = 'refresh',
}

export enum StorageFolderName {
  DOCUMENTS = 'documents',
}

export enum OrderFilter {
  ASC = 'asc',
  DESC = 'desc',
}
